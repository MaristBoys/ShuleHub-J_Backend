# Usa H2 in memoria per i test (veloce e indipendente dal PC)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate configurazione per H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=never

# Aggiungi in fondo al file dei test
# Per sicurezza, aggiungi la stessa riga anche nel file dei test 
#(in src/test/resources), così i test avranno la loro chiave dedicata
# e non cercheranno nulla all'esterno:
shulehub.jwt.secret=${JWT_SECRET_ENV:0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF}